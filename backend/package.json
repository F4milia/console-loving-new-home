{
  "name": "lnh-console-backend",
  "version": "1.0.0",
  "description": "Care Vineyard Console backend - match engine + facility API (Postgres-backed, shares the loving-new-home database)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "seed": "node src/seed.js",
    "test": "node --test \"src/**/*.test.js\""
  },
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "pg": "^8.12.0"
  }
}
