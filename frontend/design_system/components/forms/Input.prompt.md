Underline text field for the contact form — no boxed border, no rounded pill. Label sits above in small mono caps.

```jsx
<Input label="Your name" name="name" required />
<Input label="Phone" name="phone" type="tel" />
```
